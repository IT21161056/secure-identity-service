# name: Build and Deploy to Azure Container Instances

# on:

#   # push:
#   #   branches: [main]
#   # pull_request:
#   #   branches: [development]

# env:
#   RESOURCE_GROUP: resource_group_1
#   ACR_NAME: secureAuth
#   ACI_NAME: identity-service
#   LOCATION: eastus
#   CONTAINER_PORT: 5000 # Define your container's exposed port

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     environment: production

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       # Login to Azure using the stored credentials
#       - name: Azure Login
#         uses: azure/login@v1
#         with:
#           creds: ${{ secrets.AZURE_CREDENTIALS }}

#       # Build and push Docker image to ACR
#       - name: Build and push image to ACR
#         run: |
#           # Build the Docker image
#           docker build -t $ACR_NAME.azurecr.io/$ACI_NAME:${{ github.sha }} .

#           # Login to ACR using Azure CLI (already logged in via azure/login)
#           az acr login --name $ACR_NAME

#           # Push the image to ACR
#           docker push $ACR_NAME.azurecr.io/$ACI_NAME:${{ github.sha }}

#       # Deploy to Azure Container Instances
#       - name: Deploy to Azure Container Instances
#         run: |
#           # Check if the container instance exists
#           if az container show --resource-group $RESOURCE_GROUP --name $ACI_NAME --query name -o tsv; then
#             echo "Container instance '$ACI_NAME' exists. Updating..."
#             az container update \
#               --resource-group $RESOURCE_GROUP \
#               --name $ACI_NAME \
#               --image $ACR_NAME.azurecr.io/$ACI_NAME:${{ github.sha }}
#           else
#             echo "Container instance '$ACI_NAME' does not exist. Creating..."
#             az container create \
#               --resource-group $RESOURCE_GROUP \
#               --name $ACI_NAME \
#               --image $ACR_NAME.azurecr.io/$ACI_NAME:${{ github.sha }} \
#               --cpu 1 \
#               --memory 1.5 \
#               --ports ${{ env.CONTAINER_PORT }} \
#               --dns-name-label $ACI_NAME # This will create a public DNS name
#               --registry-login-server $ACR_NAME.azurecr.io \
#               --registry-username ${{ secrets.ACR_USERNAME }} \
#               --registry-password ${{ secrets.ACR_PASSWORD }} \
#               --environment-variables \
#                 NODE_ENV="production" \
#                 PORT="${{ env.CONTAINER_PORT }}" \
#                 API_BASE_URL="/api/v1" \
#               --secure-environment-variables \
#                 MONGO_URL="${{ secrets.MONGO_URL }}" \
#                 JWT_SECRET="${{ secrets.JWT_SECRET }}" \
#                 ACCESS_TOKEN_SECRET="${{ secrets.ACCESS_TOKEN_SECRET }}" \
#                 REFRESH_TOKEN_SECRET="${{ secrets.REFRESH_TOKEN_SECRET }}"
#           fi
